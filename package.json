{
  "name": "starter-grub-dash-front-end",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "echo \"You must cd to either client or sever directory before running npm start\" && exit 1"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
